# Integrations (future)

Provider-specific OAuth, token storage, and sync will live here (Meta Ads, WooCommerce, Shopify).

Phase 1 only defines `Connection` and `SyncJob` in Prisma — no live sync yet.
